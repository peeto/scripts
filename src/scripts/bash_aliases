#!/bin/bash
alias lh='ls -lh'
alias ll='ls -lh'
alias la='ls -lah'
alias lah='ls -lah'
alias nomnomnom='echo Updating repository catalogs...
echo
sudo apt-get -yu update
echo
echo Upgrading system....
echo
sudo apt-get -yfu dselect-upgrade
echo
echo Cleaning up system...
echo
sudo apt-get -yu autoclean
sudo apt-get -yu autoremove
echo
echo Done.
'
alias fucking='sudo'
alias fuck='su -'
alias oops='sudo !!'

